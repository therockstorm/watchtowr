{
  "name": "watchtowr",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "all:install": "./all.sh yarn",
    "all:lint": "./all.sh 'yarn run lint'",
    "all:test": "./all.sh 'yarn run test'"
  }
}
